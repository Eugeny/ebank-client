angular.module('services')
    .factory('currencyService', ['',
        function() {
            'use strict';

            var self = {
                
            };

            return self;
        }]);